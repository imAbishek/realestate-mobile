# This project has a pre-existing peer-dependency conflict (react-dom wants
# a newer react than expo pins, via expo-router -> vaul/@radix-ui), so every
# npm / `expo install` here must use legacy peer resolution. See DEVLOG.
legacy-peer-deps=true
